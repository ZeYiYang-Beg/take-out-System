<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">

    <select id="getSetmealIdsByDishId" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish where dish_id in
        <foreach collection="dishIds" item="df" separator="," close=")" open="(">
            #{df}
        </foreach>
    </select>
    <delete id="delete">
        delete from setmeal_dish where id in
        <foreach collection="idsn" item="Id" separator="," open="(" close=")">
            #{Id}
        </foreach>
    </delete>
    <insert id="insertBatch">
        insert into setmeal_dish (setmeal_id, dish_id, name, price, copies)
        values
        <foreach collection="setmealDishes" item="setmeal">
            (#{setmeal.setmealId}, #{setmeal.dishId}, #{setmeal.name}, #{setmeal.price}, #{setmeal.copies})
        </foreach>
    </insert>
</mapper>